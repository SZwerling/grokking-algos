const obj1 = {
    one: 'Jameson',
    two: true,
    three: [1, 2, 3]
}

const keyList = Object.keys(obj1)
const valuesList = Object.values(obj1)
const entriesList = Object.entries(obj1)

const arr1 = ['Jan', 'Feb', 'Mar']
console.log(arr1)
const missing = arr1.splice(1, 1, 'February')
console.log(arr1)
console.log(missing)