const obj1 = {
    one: 'Jameson',
    two: true,
    three: [1, 2, 3]
}

const keyList = Object.keys(obj1)
const valuesList = Object.values(obj1)
const entriesList = Object.entries(obj1)
const isKey = 'one' in obj1  // true
const isValue = Object.values(obj1).includes('Jameson') // true

console.log(isValue)